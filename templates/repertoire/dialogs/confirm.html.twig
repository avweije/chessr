<dialog id="confirmModal" class="is-relative p-6 bg-tacao-50 dark:bg-slate-700 is-large-rounded shadow-lg">
	<button id="confirmModalCloseButton" class="close-button outline-none">
		<svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 text-gray-700 hover:text-gray-900 dark:text-gray-500 dark:hover:text-gray-300" fill="none" viewbox="0 0 24 24" stroke="currentColor">
			<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
		</svg>
	</button>
	<h2 class="mb-6 mr-32 text-lg has-text-weight-semibold tc-sharp">Please confirm</h2>

	<p class="w-full tc-base">Are you sure you want to delete this move from your repertoire?</p>
	<p class="w-full tc-base my-4">All the lines that follow this move will be deleted also.</p>

	<div class="is-flex is-justify-content-flex-end space-x-2 mt-8">
		<button id="confirmModalCancelButton" class="button">
			Cancel
		</button>
		<button id="confirmModalConfirmButton" class="button is-warning">
			Confirm
		</button>
	</div>
</dialog>
